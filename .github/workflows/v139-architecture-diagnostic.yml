name: v0.7.139 Architecture and Presentation Diagnostic

on:
  push:
    branches: [main]

permissions:
  contents: read
  statuses: write

jobs:
  detail:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring
          coverage: none

      - id: arch
        shell: bash
        run: |
          ./tests/check-architecture.sh > architecture-detail.log 2>&1
          cat architecture-detail.log

      - if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs=require('fs');
            const ok='${{ steps.arch.outcome }}'==='success';
            const lines=fs.existsSync('architecture-detail.log')?fs.readFileSync('architecture-detail.log','utf8').split(/\r?\n/).filter(Boolean):[];
            const clean=(line)=>String(line||'unknown').replace(/^.*?plugin\/gloskin-site-core\//,'').replace(/[^A-Za-z0-9._=:/-]+/g,'-').slice(0,76);
            if(ok){
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'success',context:'v139/architecture-detail-pass',description:'PASS: architecture detail'});
            } else {
              const last=clean(lines[lines.length-1]);
              const prev=clean(lines[lines.length-2]);
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'failure',context:`v139/architecture-last/${last||'unknown'}`,description:'Last architecture output line'});
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'failure',context:`v139/architecture-prev/${prev||'unknown'}`,description:'Previous architecture output line'});
            }

      - id: presentation
        if: always()
        shell: bash
        run: |
          ./tests/check-presentation.sh > presentation-detail.log 2>&1
          cat presentation-detail.log

      - if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs=require('fs');
            const ok='${{ steps.presentation.outcome }}'==='success';
            const lines=fs.existsSync('presentation-detail.log')?fs.readFileSync('presentation-detail.log','utf8').split(/\r?\n/).filter(Boolean):[];
            const clean=(line)=>String(line||'unknown').replace(/^.*?plugin\/gloskin-site-core\//,'').replace(/[^A-Za-z0-9._=:/-]+/g,'-').slice(0,82);
            if(ok){
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'success',context:'v139/presentation-detail-pass',description:'PASS: presentation detail'});
            } else {
              const last=clean(lines[lines.length-1]);
              const prev=clean(lines[lines.length-2]);
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'failure',context:`v139/presentation-last/${last||'unknown'}`,description:'Last presentation output line'});
              await github.rest.repos.createCommitStatus({owner:context.repo.owner,repo:context.repo.repo,sha:context.sha,state:'failure',context:`v139/presentation-prev/${prev||'unknown'}`,description:'Previous presentation output line'});
            }
